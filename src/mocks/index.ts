import { fixedIcon100px, fixedIcon50px, fluidThumbnail600px } from "./image"
import { postHtml } from "./post"

export const mocks = {
  fixedIcon50px,
  fixedIcon100px,
  fluidThumbnail600px,
  postHtml,
}
